#!/bin/bash
PID_FILE="$HOME/.claude-memory/config/daemon-pid"
if [ -f "$PID_FILE" ]; then
  PID=$(cat "$PID_FILE")
  if kill -0 "$PID" 2>/dev/null; then
    kill "$PID"
    echo "已停止 (PID: $PID)"
  else
    echo "进程已不存在"
  fi
  rm -f "$PID_FILE"
else
  echo "没有在运行"
fi
